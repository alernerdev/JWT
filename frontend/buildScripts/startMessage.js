// ES5 sybtax
// var chalk =require('chalk');

// this is ES6 syntax.  The fact that it works indicates that
// my Babel transpiling is setup correctly, but you have to use babel-node for transpiling
import chalk from 'chalk';

/* eslint-disable no-console */

console.log(chalk.green("Starting app in dev mode..."));
